angular.module("ImageApp").directive('someDirective', [function () {
  return {
    // restrict: 'A',
    // link: function (scope, iElement, iAttrs) {
    replace: true,
    temlae: rrrr'<div>Some staff here</div>'
      
    }
  }]);
